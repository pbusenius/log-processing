import polars as pl


def detect_impossible_travel(df: pl.DataFrame, speed_threshold: int) -> pl.DataFrame:
    # group by user
    # enrich ip locations
    # shift df
    # compute distnace to last entry
    # compute km/h to last entry
    # apply threshold
    # return events
    return df
